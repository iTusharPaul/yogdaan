import React from 'react';
import LoginPage from './pages/LoginPage.jsx';

function App() {
  // In a real app, you would have logic here to check if the user
  // is already logged in and show a different component.
  // For now, we'll just show the login page.
  
  return <div> 
     <LoginPage/>
    </div>
  
}

export default App;